# 🏗️ Implementation Plan: UI Shell

## Goal
Build the static UI components and hook up the search event.

## Proposed Changes
1. Create `WeatherCard.tsx` component.
2. Create `SearchBar.tsx` component.
3. Implement `useWeather` hook with mock data.

## Verification
- Run `npm test` on Search component.
- Manual check of responsive layout on mobile.
