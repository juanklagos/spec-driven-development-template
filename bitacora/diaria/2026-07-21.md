# Daily log - 2026-07-21 / Registro diario - 2026-07-21

## Work completed / Trabajo realizado

- Cierre de la trilogía del builder aprobada con "hazlo todos" (specs 007-009, agentes delegados con verificación real cada uno):
  - **007 v2 Fácil**: semáforo del gate + badges por tarjeta, aprobar spec desde la UI, tour, galería de 4 plantillas, editor guiado con autocompletado EARS, undo/redo + PNG. 3 tools MCP nuevas.
  - **008 v3 IA**: asistente ✨ (frase → board previsualizable → bundles reales), botón "Implementar con agente" con hard stop, lint EARS en vivo (bug real de regex con acentos encontrado y corregido), prompt orquestador MCP.
  - **009 v4 Equipos**: vista Kanban desde el estado real de los .md, edges tipados con warnings de dependencia en el gate, tasks→issues vía gh (idempotente; sin issues reales en verificación), presencia por SSE.
- Release **v1.7.0** publicado (corte completo de versiones, CHANGELOG con Verified).
- Todas las verificaciones: builds raíz+builder, mcp:test extendido en cada spec, 3 scripts SDD en 0 errores, screenshots de navegador contra sandboxes frescos.

## Pending / Pendientes

- Autor: publicar 1.7.0 en npm (3 paquetes, OTP) → refrescar MCP Registry; opcional casilla Marketplace en el release; borrar `aprende-sdd-smoke`.
- Fase 3 de spec 006 (MCP App) tras la spec MCP final del 2026-07-28.
- Ideas no tomadas del backlog v2: épicas como grupos colapsables, timeline por tarjeta, import/export Obsidian/Mermaid, drag-to-approve en kanban.

## Follow-up (same day) / Seguimiento (mismo día) — "termina lo que falte"

- Instalador sidecar: INDEX limpio para workspaces nuevos (bug hallado en verificación de 007, corregido y probado).
- Plantillas de spec: sección opcional "Propiedades de la spec" (cierre del mediano plazo de la guía 50).
- **Fase 3 de spec 006 implementada con el SDK oficial** (ext-apps@1.7.4, RC congelado): tool `sdd_board_app` + recurso `ui://sdd/board.html` — el board (tarjetas, grafo SVG, semáforo, warnings) dentro de Claude/VS Code/ChatGPT; verificado por stdio y render real en navegador. Checklist del 2026-07-28 registrado en history de la 006.
- Queda del proyecto: acciones del autor (npm 1.7.0 ×3 + Registry, borrar aprende-sdd-smoke, casilla Marketplace), T7b de la 006 (demo Pages, pospuesta por diseño) y la revisión del 28-jul.

## Follow-up 2 (mismo día) — pasada editorial a la guía 51 / Editorial pass on guide 51

- Guía 51 (EN/ES) reescrita como manual fluido único con **5 capturas reales** del builder (`docs/assets/builder/`: canvas, drawer, assistant, kanban, templates) — se acabaron las secciones acumuladas "novedades v2/v3/v4". Capturas tomadas con Playwright contra un sandbox sidecar real ("Tienda de plantas": 4 specs, 2 aprobadas, edges tipados con warning `⚠ 1 dep` real, kanban con las 3 columnas pobladas).
- `site/scripts/sync-docs.mjs`: los links de imagen relativos ahora van a raw.githubusercontent (blob rompía los `<img>` del sitio). Verificado: sync con urls raw + build del sitio verde (158 páginas) + 3 scripts SDD en 0 errores. Sin commit (revisión del autor pendiente).

## Follow-up 2 (same day) / Seguimiento 2 (mismo día) — Dashboard + regla de estado única

- `/dashboard` rediseñado (workflow con panel de jueces de diseño) para igualar el builder: ejecutivo, KPIs, progreso por spec, avisos de dependencias, tokens shadcn exactos, dark/light, un solo idioma con toggle ES|EN.
- Bug de coherencia encontrado y corregido: cuatro derivaciones distintas del estado de una spec (dos con regex diferentes). Unificado en `sdd-core.specTone` y distribuido como `tone` por API y tools MCP. Regresión cubierta en `mcp:test`.
- Verificación: builds, mcp:test, 3 scripts SDD, y navegador (dashboard claro/oscuro + ES/EN; lienzo y kanban con los dos casos borde).
