# Daily log / Bitácora diaria — 2026-08-13

## Contexto inicial / Initial context

Cierre de mantenimiento del template: la política marcaba 8 warnings en los
archivos de reglas de agente (el template no pasaba su propio chequeo), y tres
specs figuraban «In Progress» en `specs/INDEX.md` aunque su estado real ya era
otro. Gate y validate en 0/0; policy en 0 errores / 8 warnings.

## Trabajo realizado / Work completed

- **Política a 0/0.** Los 8 archivos de reglas de agente (`.cursorrules`,
  `.clauderules`, `CLAUDE.md`, `GEMINI.md`, `WINDSURF.md`, `AIDER.md`, `ROO.md`,
  `.github/copilot-instructions.md`) no incluían la frase canónica de ejecución
  («recommended default workspace / espacio de trabajo recomendado por
  defecto»). Se reescribió la línea de la sección *Execution workspace* para
  contener el marcador, en ambos idiomas. Dogfooding: el template ahora pasa su
  propia `check-sdd-policy.sh` con 0 warnings.
- **Spec 023 (desk-electron) cerrada.** 12 de 12 tareas implementadas; T9
  (firma de macOS) fuera de alcance por decisión registrada
  (`bitacora/decisiones/2026-07-23-no-firmar-la-app-de-escritorio.md`). Estado
  en `INDEX.md` → `Done / Completada (T9 firma fuera de alcance por decisión)`.
- **Spec 006 (visual-spec-builder) cerrada.** Fases 1-3 y T1-T8 hechas; solo
  T7b (demo interactiva en Pages) diferida a propósito (requiere FS Access API,
  solo Chrome). Estado → `Done / Completada (T7b demo Pages diferida a
  propósito)`, mismo patrón que la 026 (T6 ticker diferido).
- **Spec 011 (one-command-launcher): prioridad alineada.** Sigue abierta por
  decisión (`bitacora/decisiones/2026-07-23-alcance-restante-de-011-tras-el-escritorio.md`
  — no se cierra por superada). Pero la decisión ordenaba bajar la prioridad a
  Media y `INDEX.md` seguía diciendo High; se corrige a `Medium / Media`.
- **Bug encontrado y corregido: `generate-status.sh` contaba `specs/_template/tasks.md`.**
  El script usaba `find … -name tasks.md`, que incluye el template con sus 3
  placeholders `- [ ]`, así que reportaba 11 pendientes cuando la realidad son
  8. El core `generateStatus` (via `listSpecs`, prefijo `NNN-`) ya excluía
  `_template`: era una divergencia bash-vs-core silenciosa. Se añade
  `-not -path '*/_template/*'` al `find`. Al regenerar, `Pending` vuelve a 8.
- `STATUS.md` regenerado.

## Validación / Validation

- `./scripts/validate-sdd.sh . --strict` → 0 errores, 0 warnings.
- `./scripts/check-sdd-policy.sh .` → 0 errores, 0 warnings (antes 8).
- `./scripts/check-sdd-gate.sh .` → 0 errores, 0 warnings; ABIERTA.

## Pendiente / Pending

- Spec 011: ruta `npx` (T3-T6, T8) sigue abierta y en Media, por decisión
  registrada; condiciones de revisión en la propia decisión.
- Nada más pendiente en el cierre de este mantenimiento.

## Riesgos / Risks

- El cambio de prioridad de la 011 se basa en la decisión del 2026-07-23; si
  hubo un re-alcance posterior que la devolviera a High, revertir.
