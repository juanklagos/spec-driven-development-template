# Daily log - 2026-07-20 / Registro diario - 2026-07-20

## Initial context / Contexto inicial

- El autor recuperó su cuenta de npm; objetivo del día: publicar los paquetes preparados y cerrar la distribución.

## Work completed / Trabajo realizado

- Preparación final npm: scope `@sdd/*` → `@juanklagos/*` (paquetes, imports, scripts de workspace, docs 35/37); scaffolder renombrado a `@juanklagos/create-sdd-project` (el nombre sin scope está tomado en npm por un tercero, v0.20.0).
- Metadatos de publicación completos y verificación previa: build, typecheck, mcp:test, `npm pack --dry-run` x3.
- Primer intento de publish del autor: 403 por política de npm (2FA obligatorio); resuelto activando 2FA en la cuenta.
- Publicados por el autor: `@juanklagos/sdd-core@1.5.0`, `@juanklagos/sdd-mcp@1.5.0`, `@juanklagos/create-sdd-project@1.5.0` (confirmado vía search API del registry; endpoints en propagación).
- Post-publicación: README EN/ES con `npx @juanklagos/create-sdd-project` como inicio sin clonar y config MCP vía npx; caveat de "no publicado" eliminado del README del scaffolder.
- Preparado el paso al MCP Registry: `sdd-mcp` 1.5.1 con campo `mcpName` (requisito de validación de propiedad del registry) y `server.json` alineado a 1.5.1; falta publicar 1.5.1 (OTP del autor) y `mcp-publisher login/publish`.

## Validation / Validaciones

- 3 scripts SDD en 0 errores; build del monorepo en verde.

## Follow-up (same day) / Seguimiento (mismo día)

- Autor publicó `@juanklagos/sdd-mcp@1.5.1` (con `mcpName`).
- MCP Registry oficial: login por device flow de GitHub (autorizado por el autor) y publicación exitosa de `io.github.juanklagos/sdd-mcp` v1.5.1 con `mcp-publisher` 1.8.0. Ajuste requerido por validación: descripción de server.json acortada a ≤100 chars.
- Distribución completa: GitHub (template + curso), Pages, Marketplace (Action), npm (3 paquetes), MCP Registry.

## Pending / Pendientes

- Borrar `aprende-sdd-smoke` (Settings del repo).

## Risks / Riesgos

- La propagación CDN de npm puede tardar minutos; verificar los enlaces de npmjs.com antes de difundir.

## Follow-up 2 (same day) / Seguimiento 2 (mismo día)

- Investigación doble sobre el builder visual de specs solicitado por el autor (¿existe? / ¿con qué se construye?).
- Veredicto: no existe; hueco real ("el Relume de las specs"). Síntesis y propuesta en `idea/PROPUESTA_BUILDER_2026-07-20.md` con opciones A (MVP 2-4 sem) y B (ambiciosa 6-9 sem).
- Pendiente: decisión del autor A/B → spec 006-visual-spec-builder antes de implementar.

## Follow-up 3 (same day) / Seguimiento 3 (mismo día) — SDD Builder Fase 1

- Autor eligió Opción B (ambiciosa); spec `006-visual-spec-builder` creada, aprobada y con consentimiento registrado.
- Fase 1 completa y verificada end-to-end contra workspace sidecar real:
  - `board.ts` en sdd-core (tasks.md parse/toggle quirúrgico, board.canvas JSON Canvas, guards, escrituras atómicas).
  - API REST en http.ts (board, spec detail, toggle de tareas, creación de specs) + estáticos `/builder` con fallback 503.
  - Frontend `builder/` (agente delegado, verificación con screenshots reales): canvas React Flow, tarjetas con estado y progreso, uniones con labels editables, paleta, drawer de tareas, dark mode, estados vacío/error, bilingüe.
- Bug real encontrado por el agente y corregido: `createSpec` dejaba bundle parcial sin `_template`; ahora limpia atómicamente (verificado).
- README EN/ES: instrucciones del builder (`npm run builder:build`). Pendiente: Fases 2 (sync vivo), 3 (MCP App), 4 (demo+guía).

## Follow-up 4 (same day) / Seguimiento 4 (mismo día) — Builder Fase 2 (sync en vivo)

- SSE `/api/events` con watcher recursivo debounced (filtra temporales de escritura atómica); múltiples clientes, keep-alive + evento `ping`, arranque perezoso sin crash si specs/ no existe.
- Front: reconciliación por id estable sin pisar posiciones locales, guard anti-eco, indicador 🟢 En vivo con backoff, watchdog de 60s (hallazgo: proxies pueden tragarse el cierre y EventSource nunca dispara error), banner ámbar de workspace cambiado, hint con SDD_PROJECT_ROOT.
- Verificado por agente con evidencia: curl del stream (hello/change/ping), tarjeta 0/3→3/3 actualizándose sola en screenshots, banner de workspace tras relanzar server con otro root.
- Pendientes honestos documentados: estado "Live off" no observado visualmente (túnel), specs borradas no retiran su tarjeta (conservador).

## Follow-up 5 (same day) / Seguimiento 5 (mismo día) — Release v1.6.0

- Fase 3 (MCP App) pospuesta con decisión registrada hasta la spec MCP final (2026-07-28).
- Fase 4 ejecutada: guía 51 EN/ES (SDD Builder visual guide) + índice + llms.txt regenerado; corte v1.6.0 (versiones, CHANGELOG con Verified, roadmap, badges) y release publicado vía gh.
- Pendiente del autor: publicar 1.6.0 de los 3 paquetes en npm (con OTP) y refrescar el MCP Registry; opcional marcar el release al Marketplace desde la UI.
