# Daily log / Bitácora diaria — 2026-08-08

## Hecho hoy / Done today

- Spec 028 (superficie completa MCP + builder) cerrada: T1-T9 marcadas, INDEX a `Done / Completada`, STATUS regenerado.
- T8 (verificación) completada: 110 tests en verde, `typecheck` y `build` de ambos paquetes, smoke stdio con el contrato de 35 herramientas, smoke HTTP (los 6 endpoints REST nuevos contra un workspace externo temporal), `test-mcp-integration.mjs`, build del Builder y `validate-sdd.sh` con 0/0. Gate ABIERTA con 28 specs aprobadas y consentidas.
- E2E adicional de las 7 herramientas MCP nuevas por stdio contra un proyecto externo temporal recién sidecar-instalado (`sdd_install_sidecar` → `check_policy`, `legacy_discovery`, `write_spec_document`, `rename/move/remove_task`, `update_spec_status`). Las 7 pasan, incluidos los tres rechazos esperados (documento fuera de la lista blanca, línea fuera de rango, spec sin fila en INDEX) y la propiedad de diff de una sola línea sobre `INDEX.md`.
- T9 (docs) completada: las 7 herramientas nuevas y los 6 endpoints REST documentados en la referencia MCP ES/EN; las 4 capacidades nuevas del Builder (puntaje + resumen EARS, añadir tarea, bitácora desde el lienzo, informes STATUS/roadmap) documentadas en la guía visual ES/EN con sus filas en la tabla «lienzo → disco».

## Notas / Notes

- El E2E de las 7 herramientas se ejecutó como verificación puntual, no como script permanente: el contrato de superficie ya lo pinea `scripts/smoke-test-mcp.mjs` y los endpoints ya tienen cobertura permanente en `scripts/smoke-test-mcp-http.mjs` (`checkSpec028Routes`). Si se quiere cobertura de comportamiento permanente para las 7, el sitio natural es `scripts/test-mcp-integration.mjs`, que hoy tampoco cubre las 7 de la spec 027.
- El emoji del botón de bitácora en la barra superior es 📖, no 📓 (verificado en `builder/src/components/TopBar.tsx:336` antes de documentarlo).

## Siguiente / Next

- Decidir si el E2E de comportamiento de las herramientas 027 + 028 entra como cobertura permanente en `test-mcp-integration.mjs`.
- Nada pendiente en la spec 028.
