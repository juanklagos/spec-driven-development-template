# Daily log - 2026-07-22 / Registro diario - 2026-07-22

## Work completed / Trabajo realizado

- **«Este comando no funciona»**, reportado por el autor sobre `npx @juanklagos/sdd-mcp --http`
  en `~/www/larepolla`. No era su carpeta ni su instalación: el sidecar lo puso
  `create-sdd-project` 2.2.1 y el servidor detecta bien ese `projectRoot`. Lo que recibía eran
  **0 bytes y exit 0**.
  - Causa, encontrada reproduciendo: npx reutilizó su caché para la spec sin versión y ahí vivía
    la **2.2.0**, que no conoce `--http` (esa bandera llegó en la 2.2.1, spec 020). La 2.2.0 la
    ignoró, arrancó stdio, no encontró stdin y terminó. Su `dist/index.js` no contiene la cadena
    `http` ni una vez. Con `@2.2.1` el lienzo arrancó a la primera: `/builder` 200 y `/api/board`
    devolviendo `projectRoot: /Users/juanklagos/www/larepolla`.
  - **Documentación (sin spec, no es código):** todos los comandos publicados fijan `@latest` —
    `QUICKSTART.md`, los dos README, `START_HERE_NON_TECH.md`, la guía 51 EN/ES y el README del
    paquete—, incluidas las configuraciones MCP de Codex y Gemini CLI, que pasaban
    `"@juanklagos/sdd-mcp"` a pelo. Coste aceptado y anotado: `@latest` consulta el registro en
    cada arranque; se prefiere a un fallo mudo.
  - De paso, la sección «Conecta tu agente» de la guía 51 seguía mandando `npm run mcp:http:start`
    para el conector HTTP de Claude Desktop/ChatGPT — el mismo defecto que arreglé horas antes en
    el Inicio rápido, en otra sección del mismo archivo. Ahora es el comando npx real.
  - **Spec 021 abierta (pendiente de aprobación)** para lo que de verdad convirtió el desajuste en
    «no funciona»: el binario no dice nada. Medido contra la **2.2.1**, no contra la vieja:
    `--htp` → 0 bytes, exit 0; **`--help` → 0 bytes, exit 0**; y con stdin abierto en vez de
    `/dev/null` no muere, se **cuelga** indefinidamente. Segundo defecto: con el puerto ocupado
    imprime `uncaught exception (server keeps running)` y **el proceso muere con exit 0** — las
    dos mitades de esa línea son falsas, y un script que mire el código concluye que el lienzo
    está arriba.

- **Inicio rápido de la guía 51 (builder visual)**, reportado por el autor como «ese inicio rápido
  nadie lo entiende». Tenía razón y el defecto era peor que la redacción: la sección enseñaba la
  ruta de contribuyente —`npm run builder:build`, `install-spec-sidecar.sh`, `npm run
  mcp:http:start`— que **solo funciona dentro de un clon de este repositorio**, justo lo que la
  spec 020 acababa de dejar de ser necesario. Quien instalaba desde npm no tenía ninguno de esos
  comandos.
  - Reescrita a los dos comandos reales de la spec 020, verificados ejecutándolos, no leyéndolos:
    `npx @juanklagos/create-sdd-project <dir|.>` sin TTY creó el sidecar y anunció sus valores por
    defecto; `npx @juanklagos/sdd-mcp --http` desde la carpeta del proyecto imprimió las tres URLs
    con el lienzo primero, `/builder` respondió 200 con el HTML del builder, y `/api/board` devolvió
    la spec `001-catalogo` creada a mano en ese workspace de prueba.
  - Ahora es paso a paso y separa los dos casos que antes no se distinguían: proyecto nuevo
    (`create-sdd-project mi-app`) y proyecto que ya existe (`create-sdd-project .`, que solo añade
    `spec/` y no mueve nada). Añade Node 20 como único requisito, dice que `/mcp` no es una página
    —el tropiezo 2 de la 020 en su forma documental—, y cubre puerto ocupado y `SDD_PROJECT_ROOT`.
  - La ruta larga no se borró: vive en un `<details>` rotulado para contribuyentes al template, con
    su motivo (el builder está bloqueado contra la raíz del template a propósito).
  - ES y EN a la vez; `site/scripts/sync-docs.mjs` y la construcción del sitio pasan, y el
    `<details>` renderiza en la página construida.

- **Spec 016 (puerta de entrada)** cerrada: sello de versión versionado y escrito por los dos
  andamiadores, aviso de consentimiento heredado apoyado en `installed_at`, `validate-sdd.sh` que ya
  no aborta con la idea vacía, idea y bitácora vacías en proyectos nuevos, y `demo.tape` andamiando
  antes de ejecutar (el GIF de portada grababa su propio primer comando fallando).

- **Spec 017 (menú de la documentación)**, reportada por el autor como «el menú se ve todo revuelto
  y mezcla los dos idiomas». Medido sobre `site/dist` antes de tocar nada: el menú español traía
  **103 entradas en vez de 52**, todas bajo `/es/`, es decir la mitad eran las guías inglesas
  servidas como respaldo en español.
  - Causa: **Starlight empareja traducciones por ruta de archivo, no por número de guía.**
    `00-introduccion.md` y `00-introduction.md` reducen a rutas distintas, así que las trataba como
    dos páginas. La prueba estaba en el propio sitio: `09-release-checklist.md` es la única guía con
    nombre idéntico en ambos idiomas y la única que no se duplicaba (52 + 52 − 1 = 103, el conteo
    exacto medido).
  - Arreglo: ambos idiomas se publican bajo el mismo nombre de archivo, emparejados por número; y el
    menú autogenerado plano pasa a siete grupos curados con etiqueta traducida ES/EN.
  - El reparto se declara **una sola vez** en `site/src/guides.mjs`, que importan el sincronizador y
    `astro.config.mjs`, con una comprobación que **falla la construcción** si una guía queda fuera
    del menú, cae en dos grupos, o pierde su par en el otro idioma. Probada en negativo.
  - Trampa evitada: la URL no es el nombre del archivo. Astro lo pasa por `github-slugger`, que borra
    los puntos, así que `39-v1.2.0-preparation.md` se sirve en `39-v120-preparation/`. El menú y las
    redirecciones usan esa misma función en vez de codificar los dos casos a mano.
  - Las URLs españolas cambian (`/es/guides/00-introduccion/` → `.../00-introduction/`), decisión
    aprobada en chat, con 51 redirecciones generadas a partir de los nombres de archivo para que
    ningún enlace publicado se rompa.
  - Verificado sobre `dist`, no sobre la intención: 52 entradas en el menú español y 52 en el inglés,
    cero cruzadas; títulos e insignias en el idioma de la página; solo «Empieza aquí» desplegado (más
    el grupo de la página actual, que Starlight abre solo); cero enlaces internos rotos en las 107
    páginas construidas.

- Verificaciones: `npm run build` en `site/` limpio, y los scripts SDD (`validate-sdd`,
  `check-sdd-policy`, `check-sdd-gate`) en 0 errores. `score-spec` da 68/100 a la spec 017, misma
  banda y mismas dos notas de palabra clave que la 016.

- **Spec 018 (logo)**. El proyecto no tenía marca: el favicon era el destello de Astro sin tocar. Se
  descartaron dos rondas de conceptos y quedó un monograma de la `s` construido con tres barras y dos
  conectores, **recortado en negativo** dentro de un cuadrado, en monocromo.
  - El monocromo no es estética: dentro del producto el verde `#16a34a` ya significa **estado**
    (compuerta abierta, semáforo del gate, insignias). Un logo verde pondría marca y estado a
    competir por el mismo color.
  - El recorte se eligió por dónde vive el logo: pestañas, npm y avatares, a 16-32 px sobre fondos
    ajenos. Una letra suelta no tiene silueta propia y ahí se deshace.
  - Favicon adaptativo por `prefers-color-scheme`; la cabecera con dos archivos fijos, porque un SVG
    embebido como imagen no ve el `data-theme` del sitio y seguiría al sistema operativo. Verificado
    justo en ese caso: sistema en claro, sitio en oscuro, y el logo cambia.

- **Spec 018, ampliación**: el logo también en el builder, sustituyendo al 🌱 de su cabecera y de su
  favicon. Ahí el SVG va **en línea con `currentColor`**: un solo dibujo para los dos temas, porque
  dentro del builder el símbolo está en el mismo documento y hereda el color del texto. Es justo lo
  contrario del sitio, donde va embebido como imagen, no ve el tema de la página y necesita dos
  archivos de color fijo. La misma marca, dos técnicas, según dónde vive cada una.
  - Verificado que nada dependía del texto `🌱 SDD Builder`, y que la copia del builder dentro del
    paquete MCP no está versionada: `prepack` la regenera, así que el paquete publicado recogerá el
    logo solo.
  - El panel del servidor MCP también lleva el 🌱 en su título y en su marca. Queda pendiente a
    propósito: es otra superficie.

- **Spec 019 (enlaces)**, defecto encontrado mientras se verificaba el logo, leyendo el árbol de
  accesibilidad de una página. El sincronizador reescribía un enlace entre guías **dos veces**:
  `](./19-matriz-prompts-por-objetivo.md)` pasaba a `](../19-prompt-matrix-by-goal/)` por la regla de
  guías, y después la regla general lo volvía a capturar y lo mandaba a
  `github.com/.../blob/main/docs/19-prompt-matrix-by-goal/`, que no existe. **140 enlaces rotos en 54
  archivos**, 19 rutas distintas.
  - Es anterior a la spec 017 —antes rompía igual, solo cambiaba el slug—, pero **la verificación de
    la 017 declaró «cero enlaces rotos» mirando solo los internos**, y estos salen del sitio. El
    defecto existía y el barrido no podía verlo.
  - Arreglo: cada regla que resuelve un enlace lo aparta tras un marcador y se devuelve al final, así
    que el orden de las reglas deja de importar. Si un marcador sobreviviera, el sincronizador falla
    en vez de publicar texto ilegible.
  - La regla cruzada entre idiomas sobrevivía **por casualidad** —su ruta se sale del repositorio y
    una guarda pensada para otra cosa la devolvía intacta—. Se aparta igual: depender de eso era una
    trampa esperando a que alguien cambiara la profundidad del enlace.
  - El barrido nuevo (670 enlaces y recursos del sitio construido, relativos incluidos, frente a los
    570 absolutos de antes) destapó dos rotos más de otra causa: `<a href>` de HTML crudo dentro del
    markdown, que ninguna regla miraba porque todas exigen sintaxis markdown. Cubiertos también.
  - Medido al cerrar: 140 rotos → **0**; los 70 enlaces a GitHub que ya resolvían siguen resolviendo;
    cero marcadores supervivientes; cero rotos en las 158 páginas construidas.

- **Spec 022 (lista de tareas plegable en el panel del builder)**, reportada por el autor con captura
  sobre `001-login` de `~/www/larepolla`: «cuando hay muchas tareas no deja ver el resto». Medido en el
  lienzo, esa spec tiene **52 tareas**, no las 20 que se veían en la captura.
  - No era un fallo de scroll —el `ScrollArea` de `SpecDrawer.tsx:568` funciona— sino de jerarquía:
    `:586` pintaba las 52 tareas sin techo, y el panel de issues y el extracto de `spec.md` quedaban
    varias pantallas por debajo. La lista de tareas era la sección **más larga y más variable** del
    panel y la única que no se podía plegar, mientras la pestaña vecina «Editar spec» pliega sus siete
    secciones con el mismo `Accordion` que ya vive en el repositorio (`SectionEditor.tsx:280`).
  - Segundo defecto que delataba la captura: la cabecera decía solo «TAREAS» mientras el nodo del
    lienzo, para esa misma spec, decía «0/52 tareas». Al plegar, la cabecera es lo único que queda, así
    que ahora lleva el recuento (`0/52 hechas`, ES/EN).
  - El recuento **no añade estado**: se deriva de `detail.tasks`, que cada marcado ya refresca. El
    `HelpHint` va fuera del disparador a propósito: es un `PopoverTrigger`, o sea un `button`, y no
    puede anidarse dentro del `button` del acordeón.
  - Decisiones tomadas y aprobadas en chat: **una sola preferencia** para todo el builder, no una por
    spec; y **desplegado por defecto**, de modo que quien nunca la pliegue vea exactamente lo de hoy.
    Persistida en `localStorage`, el mismo patrón del idioma (`i18n.ts:17`) y del tour (`store.ts`).
  - Verificado en el lienzo real contra `larepolla`, no leyendo el diff: plegada, issues y extracto
    caben **sin scrollear**; abrir la 002 la encuentra plegada; tras recargar sigue plegada; marcar una
    tarea escribió el `- [x]` en `tasks.md` y movió el recuento a `1/52` en el panel y en el nodo. Esa
    tarea se desmarcó después y el `tasks.md` de `larepolla` quedó **idéntico byte a byte** al original.
  - No verificado en el lienzo: el estado vacío «Sin tareas» (ninguna spec de `larepolla` tiene cero
    tareas). Es el mismo marcado de antes, movido fuera del plegable, pero conviene mirarlo cuando
    aparezca una spec sin tareas.
  - De paso, y sin arreglarlo: al arrancar un segundo servidor sobre el puerto 3334 se reprodujo tal
    cual el defecto **D2 de la spec 021** — `EADDRINUSE`, la línea «server keeps running» y el proceso
    muerto igualmente.

## Pending / Pendientes

- El sitio publicado en GitHub Pages se actualiza en el próximo despliegue; lo verificado aquí es la
  construcción local.
- Los grupos reparten las 52 guías de hoy. Cada guía nueva tendrá que entrar en uno: la construcción
  falla si no, que es justo lo que se quería.
