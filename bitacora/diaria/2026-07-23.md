# 2026-07-23

## Spec 023 - SDD Desk publicado, y cerrado salvo la firma

SDD Desk 0.1.0 y 0.1.1 publicados. La spec queda con 12 de 13 tareas hechas; la única
abierta es T9 (R8), que necesita credenciales de firma y no código.

**T10 (R9) — aviso de actualización.** `electron-updater` apuntando al tag fijo
`desk-latest` con el proveedor `generic`: el proveedor `github` resuelve el "latest" del
repositorio, que el escritorio deliberadamente no posee. Nunca fuerza — `autoDownload` y
`autoInstallOnAppQuit` en falso, la comprobación es muda salvo que haya algo que decir. En
macOS no descarga, porque Squirrel.Mac verifica la firma y rechaza un bundle sin firmar:
abrir la página de descarga es lo honesto en vez de correr una descarga condenada a fallar
en el último paso.

**T8 — paridad y agente externo.** `/api/board` y `/api/gate` idénticos byte a byte entre
la app y la ruta de navegador sobre el mismo proyecto. Un agente conectado al endpoint MCP
de la app creó una spec: llegó a disco, el SSE emitió `change` y quedó visible en el board.
La comprobación vive en `scripts/verify-desk-agent-write.mjs`.

**T13.** La app empaquetada arranca y sirve `/builder` con `node` y `npm` fuera del `PATH`.

**El bug de la sesión, y solo existía empaquetado.** `app.setName()` no mueve `userData`:
Electron lo resuelve desde el nombre del paquete antes de que esa línea corra. La build
empaquetada guardaba sus ajustes en `sdd-desk` y la de desarrollo en `SDD Desk`, así que la
app recordaba un proyecto distinto según cómo se lanzara — y en desarrollo no se habría
visto nunca. Ahora la ruta se fija con `app.setPath`. Apareció probando el `.app` real, no
leyendo el código.

**Diseño del release, verificado dos veces.** Un release de vida larga bajo el tag fijo
`desk-latest`, `latest=false`, con los assets reemplazados en cada versión. La 0.1.1
confirmó que las URLs de descarga siguen sirviendo y que `v2.1.0` conserva el "Latest" del
template.

## Lo que queda

T9: firma y notarización. Apple Developer ($99/año) y certificado de Windows, donde la
relicencia a MIT reabre la vía gratuita de SignPath — por verificar contra sus términos.
Hasta entonces la página de descarga advierte sin suavizar, y el escenario 7 de la spec no
se cumple.

Sigue abierto de la 011: el spike del `.mcpb` no tiene veredicto de host.

---

## Sesión de tarde — Evaluación del builder, auditoría y backlog v3 (Claude)

**Objetivo.** El propietario pidió evaluar el proyecto, proponer ideas nuevas para
el builder (qué más puede llevar, qué herramientas) contra proyectos en tendencia
con IA, y luego «hazlo todo».

**Barrido de tendencias (verificado, con fuentes).** El mercado de SDD convergió en
«markdown + slash commands + un agente»: Spec Kit (~90k★), OpenSpec (~52k★), Kiro,
BMAD, Tessl, Cursor Plan Mode, Antigravity. **Ninguno tiene lienzo** — ese es el foso
del template. El hueco que todos admiten: la deriva spec↔código, que todos resuelven
con comandos manuales que los equipos olvidan.

**Hallazgo crítico del estándar MCP.** Verificado en modelcontextprotocol.io/specification/draft/deprecated:
`Sampling`, `Roots` y `Logging` quedaron deprecados (SEP-2577, versión 2026-07-28);
`HTTP+SSE` ya estaba deprecado (SEP-2596). La ruta de migración de sampling es
«integra directamente con APIs de LLM» — justo lo que el builder rechazó. El builder
**ya** usa Streamable HTTP, así que ahí no hay deuda. Esto convirtió el «spike de
sampling» propuesto en su contrario: un registro que **confirma** el copy-first.

**Trabajo realizado.**
- Auditoría de la 011: T2 (por spec 023, `ec7c091`) y T7 (`3db6619`) estaban hechas
  sin marcar. Corregido `tasks.md`, re-alcance a la ruta `npx`, estado `Draft`→`In Progress`.
- `STATUS.md` regenerado (estaba al 2026-07-21, sin las specs 012-023). `INDEX.md`
  corregido: 011 y 023 ya no dicen «Borrador» siendo specs con trabajo en `main`.
- Spec 024 «núcleo con pruebas» (Vitest sobre `convert`/`board.specTone`/`ears`) —
  cierra la incoherencia «predica criterios verificables, no tiene ninguno ejecutable».
- Spec 025 «semáforo de deriva spec↔código» (git log × ámbito × fecha, sin LLM) —
  el diferencial del lienzo aplicado al problema no resuelto del sector.
- Backlog `idea/IDEAS_BUILDER_V3_2026-07-23.md` con 13 ideas priorizadas + descartes.
- 3 decisiones nuevas: alcance restante de 011; sampling deprecado confirma copy-first;
  (más el índice de `bitacora/decisiones/README.md`, que también estaba desactualizado).

**Decisiones tomadas.** No hacer el spike de sampling (deprecado). No migrar a tldraw
(el lienzo es grafo tipado, no dibujo libre). 024 antes que 025 (dependencia dura).

**Validación.** `validate-sdd.sh` 0/0. `check-sdd-policy.sh` 0/0. Gate ABIERTO;
3 warnings esperados (021, 024, 025 sin aprobar — el gate debe seguir cerrado para ellas).

**Bloqueos.** Ninguno. Las specs 024/025 esperan aprobación del propietario (hard stop:
no se implementa nada hasta entonces).

**Próximo paso.** El propietario revisa y aprueba (o ajusta) 024 y 025. Al aprobar,
`/sdd:gate` y a implementar 024 primero.

**Responsable.** Juan Carlos Alvarez Lagos / Claude.

---

## Sesión de tarde (cont.) — Implementación 024 y tema de docs 026 (Claude)

**024 — núcleo con pruebas (cerrada).** Aprobada con «arranca», consentida. Vitest 3
en la raíz (workspace) y en `builder/`. **57 pruebas verdes**: `board` (regla de
aprobación + `specTone` con el anti-patrón + `getBoardView` sobre workspace temporal),
`convert` (round-trip board↔canvas), `ears`, y el espejo `sections` contra una tabla
compartida. `npm test` = unitarias + smoke MCP, verde. Paso de CI añadido. T9 (prueba de
la prueba): regresión deliberada en `specTone` → 2 rojas exactamente en el anti-patrón →
revertida. Tests excluidos del `dist`.

**026 — tema de la documentación (cerrada, T6 diferido).** El propietario compartió
`tools.rmv.fyi` y pidió replicar «ese tipo de diseño» para la docs «con nuestros colores».
Revisión a fondo: delphitools = shadcn/ui + iA Writer Quattro (OFL) + esquinas rectas. La
docs es Starlight 0.41 sin CSS propio. Eligió «verde primario» (ajuste registrado de la 018,
que reservaba el verde para estado) y «construir». Hecho: iA Writer Quattro auto-hospedada
(4 pesos + OFL), `theme.css` mapeando los tokens oklch del builder a las variables de
Starlight (claro/oscuro), `border-radius:0`, asides con tratamiento, nav activo con barra
verde, logo monocromo intacto. Verificado en dev server (claro y oscuro) y build en verde;
fuentes locales, cero peticiones externas. Ticker diferido a propósito (exige ejectar
componente de Starlight).

**Decisiones nuevas:** verde primario en la docs (ajuste de 018).
**Validación:** validate 0/0, policy 0/0, gate ABIERTO (2 warnings: 021 y 025 sin aprobar).
**Próximo paso:** revisar el tema en vivo; 025 (semáforo de deriva) sigue pendiente de aprobar.

---

## Sesión de tarde (cont. 2) — Implementación 025, semáforo de deriva (Claude)

**025 — semáforo de deriva spec↔código (cerrada, T7 dashboard diferido).** Aprobada
con «continue», consentida. El diferencial de la evaluación: lo que Spec Kit/OpenSpec
resuelven con comandos que se olvidan, aquí es un color que aparece solo en el lienzo.

`packages/sdd-core/src/drift.ts` — `computeSpecDrift`: `git log` × ámbito de archivos ×
fecha de aprobación, **sin LLM ni red** (coherente con la decisión del 2026-07-20).
Estados `clean/drifted/unscoped/unknown`, degradación limpia (no-git, fecha placeholder,
sin ámbito), nunca lanza. `extractApprovalDate` nuevo (no toca `SpecSummary`, cuya forma
`sdd_list_specs` asserta en 5 campos). `drift` en `BoardSpecCard` calculado en
`getBoardView` como `tone`; `specDriftSchema` en el schema MCP (el guardrail de integración
lo exigía). UI: chip ámbar `🔀 N` en `SpecNode` y panel de commits en `SpecDrawer`, ES/EN.

**68 pruebas verdes** (8 de deriva contra repo git real + `getBoardView` end-to-end + 57
previas). Integración MCP en verde. **Verificado en vivo**: workspace git de demo con
`001-pagos` drifted (commit `d38b3c6` posterior a `src/pay/`) y `002-perfil` clean; el
drawer mostró el commit exacto. Docs guía 51 EN/ES.

**Detalle:** el gate tenía un falso positivo (grep crudo de `YYYY-MM-DD`) que saltaba
porque R2 mencionaba el literal; reescrito R2 sin él, sin tocar el gate.

**Validación:** validate 0/0, policy 0/0, gate ABIERTO (1 warning: 021 sin aprobar),
`npm test` completo verde, typecheck de los tres paquetes + builder en verde.
**Próximo paso:** 021 sigue en borrador; el dashboard HTML de deriva (T7) queda como mejora.
