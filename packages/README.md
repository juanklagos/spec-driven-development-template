# Packages

This directory contains productized implementations derived from the framework:

- `sdd-core`: reusable domain logic
- `sdd-mcp`: MCP server built on top of `sdd-core`

Root framework guidance, templates, and scripts remain the canonical source.

