# Change history / Historial de cambios

| Date / Fecha | Change type / Tipo de cambio | Summary / Resumen | Files impacted / Archivos impactados | Owner / Responsable |
|---|---|---|---|---|
| 2026-07-23 | Scope / Alcance | Borrador inicial. Nace de la auditoría del builder solicitada por el propietario: `find builder/src packages/*/src -name '*.test.*'` devuelve cero. La spec ataca la incoherencia entre la tesis del template («criterios verificables antes de código») y la ausencia de verificación ejecutable sobre su propia lógica de dominio (`convert`, `store`, `ears`, `board.specTone`). Fundacional para la spec 025. | `spec.md`, `plan.md`, `tasks.md`, `research.md` | Claude |
| 2026-07-23 | Implementation / Implementación | Aprobada y consentida («arranca»). Vitest 3 instalado en la raíz (workspace) y en `builder/`. **57 pruebas verdes** en 4 archivos: `board.test.ts` (23: tabla de aprobación de 17 casos con negaciones, `specTone` con el anti-patrón «marcadas sin aprobar», y `getBoardView` sobre workspace SDD temporal), `convert.test.ts` (8: round-trip board↔canvas + edges tipados), `ears.test.ts` (9: esqueleto EARS y palabras vagas ES/EN), `sections.test.ts` (17: espejo contra la tabla compartida `approval-cases.fixture.ts`). Tests excluidos del `tsc` (no ensucian `dist`, verificado). `npm test` = unitarias + smoke MCP, verde de punta a punta. Paso de CI añadido a `mcp.yml`. Docs EN/ES en ambos README. **T9 (prueba de la prueba):** una regresión deliberada en `specTone` puso 2 pruebas en rojo exactamente en `001-pendiente`→`done`; revertida, 57 verdes. **Nota de alcance R6:** el gate bash se mantiene offline/estructural; la exigencia de verde vive en `npm test` + CI, «su equivalente» según el propio texto de R6 — meter vitest en el gate shell lo haría depender de node_modules y perder su diseño offline. | `package.json`, `packages/sdd-core/{package.json,tsconfig.json,src/board.test.ts,src/approval-cases.fixture.ts,README.md}`, `builder/{package.json,tsconfig.json,src/{convert,ears,sections}.test.ts,README.md}`, `.github/workflows/mcp.yml` | Juan Carlos Alvarez Lagos / Claude |
