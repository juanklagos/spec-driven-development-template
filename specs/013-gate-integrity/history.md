# Change history / Historial de cambios

| Date / Fecha | Change type / Tipo de cambio | Summary / Resumen | Files impacted / Archivos impactados | Owner / Responsable |
|---|---|---|---|---|
| 2026-07-21 | Scope / Alcance | Borrador inicial. Cuatro defectos verificados contra el código: la mitigación `TEMPLATE_VERSION` que se escribe y no lee nadie, `sdd_project_root` roto en worktrees y submódulos, ningún andamiador escribiendo `.gitignore`, e `init-project.sh` copiando el INDEX del framework. | `spec.md`, `plan.md`, `tasks.md`, `research.md` | Juan Carlos Alvarez Lagos |
| 2026-07-21 | Approval / Aprobación | Spec aprobada por el autor en chat. Consentimiento registrado en `.sdd/user-consent.log`. | `spec.md` (bloque de estado) | Juan Carlos Alvarez Lagos |
| 2026-07-21 | Scope / Alcance | Se decide **no revertir** la decisión del 2026-07-21 sobre los scripts del consumidor. Su razón sigue en pie; lo que falta es la mitigación que ella misma declara. El disparador de revisión que define se cumplió con el arreglo de corrección de la spec 012, y la respuesta que el propio registro nombra para ese caso es avisar, no imponer. | `spec.md` (Contexto, Fuera de alcance), `research.md` (decisión 1) | Juan Carlos Alvarez Lagos |
| 2026-07-21 | Implementation / Implementación | **Desviación del plan, registrada**: en vez de `git rev-parse --show-toplevel` se usa `[ -e ]` en un helper `sdd_is_git_root`. `git rev-parse` devolvería la raíz del repositorio contenedor, que en un monorepo o repo anidado NO es el proyecto — habría cambiado qué directorio se devuelve, no solo arreglado el fallo. `-e` cubre worktree y submódulo (donde `.git` es archivo) conservando la semántica exacta. | `scripts/lib/sdd-root.sh` | Claude |
| 2026-07-21 | Implementation / Implementación | **Desviación del plan, registrada**: `sdd-gitignore.sh` se consolidó como `sdd-scaffold.sh` junto al escritor de índice vacío. Dos libs de veinte líneas con los mismos dos llamadores es fragmentación, no cohesión: ambas responden a la misma razón de cambio, la forma de un workspace recién creado. | `scripts/lib/sdd-scaffold.sh` | Claude |
| 2026-07-21 | Implementation / Implementación | H1-H3 completos. `sdd_is_git_root` con `-e`; `sdd-scaffold.sh` con `sdd_ensure_gitignore` (idempotente, aditiva, preserva lo del usuario) y `sdd_write_empty_spec_index`; ambos andamiadores sourceándola; `init-project.sh` deja de copiar el INDEX del framework; `action.yml` lee `TEMPLATE_VERSION` y avisa sin tocar el código de salida. La decisión del 2026-07-21 actualizada: su mitigación pasa de enunciada a implementada. | `scripts/lib/{sdd-root,sdd-scaffold}.sh`, `scripts/{install-spec-sidecar,init-project}.sh`, `action.yml`, `bitacora/decisiones/2026-07-21-action-usa-los-scripts-del-consumidor.md` | Claude |
| 2026-07-21 | Implementation / Implementación | Cobertura de los cuatro arreglos, cada uno verificado en negativo revirtiendo el arreglo: raíz en worktree, idempotencia del `.gitignore`, orden de la regla y su excepción (sin él git seguiría ignorando el registro de consentimiento) e índice sin filas ajenas. Comparación de versiones probada en cinco casos, incluido `2.10.0` contra `2.9.0`, que un orden alfabético invierte. | `scripts/test-mcp-integration.mjs` | Claude |
| 2026-07-21 | Fix / Corrección | La prueba del worktree asumía una máquina de desarrollo: creaba un commit sin identidad de git y el runner de CI la rechaza con `empty ident name` (exit 128). Identidad pasada con `-c` por invocación, sin tocar la configuración del runner. Reproducido en local con `HOME` y `GIT_CONFIG_GLOBAL` neutralizados antes de dar por bueno el arreglo. | `scripts/test-mcp-integration.mjs` | Claude |
