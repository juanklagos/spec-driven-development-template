# Change history / Historial de cambios

| Date / Fecha | Change type / Tipo de cambio | Summary / Resumen | Files impacted / Archivos impactados | Owner / Responsable |
|---|---|---|---|---|
| 2026-07-23 | Scope / Alcance | Borrador inicial. Nace de la evaluación de tendencias solicitada por el propietario: Spec Kit (`/speckit.reconcile`, `sync.analyze`) y OpenSpec (`/opsx:sync`) resuelven la deriva spec↔código con comandos manuales que los equipos olvidan; el template tiene lo que a ellos les falta (un lienzo) y el dato (la sección «Ámbito de archivos»). Cruzar ámbito × `git log` × fecha de aprobación es deriva calculable sin LLM ni red, coherente con `bitacora/decisiones/2026-07-20-builder-sin-llamadas-a-llm.md`. Depende de la spec 024 (pruebas). | `spec.md`, `plan.md`, `tasks.md`, `research.md` | Claude |
| 2026-07-23 | Implementation / Implementación | Aprobada («continue») y consentida. `packages/sdd-core/src/drift.ts`: `computeSpecDrift` puro y local (git log × ámbito × fecha de aprobación, sin LLM ni red), con estados `clean/drifted/unscoped/unknown` y degradación limpia (no-git, fecha placeholder, sin ámbito). `extractApprovalDate` nuevo en `workspace.ts` (no se añade a `SpecSummary` porque `sdd_list_specs` asserta 5 campos). `drift` en `BoardSpecCard` calculado en `getBoardView`; `specDriftSchema` en `schemas.ts` (el guardrail de integración lo exigía). UI: chip ámbar `🔀 N` en `SpecNode` (solo `drifted`) + panel de commits en `SpecDrawer`, i18n ES/EN. **68 pruebas verdes** (8 de deriva contra repo git real + `getBoardView` end-to-end + las 57 previas). Integración MCP en verde. **Verificado en vivo** sobre un workspace git de demo: `001-pagos` drifted con el commit `d38b3c6` posterior a `src/pay/`, `002-perfil` clean. Docs guía 51 EN/ES. **T7 dashboard HTML diferido** (la tool MCP ya expone drift). Falso positivo del gate (grep de `YYYY-MM-DD`) evitado reescribiendo R2 sin el literal. | `packages/sdd-core/src/{drift.ts,drift.test.ts,workspace.ts,board.ts,board.test.ts,index.ts}`, `packages/sdd-mcp/src/schemas.ts`, `builder/src/{types.ts,i18n.ts,components/SpecNode.tsx,components/SpecDrawer.tsx}`, `docs/{en,es}/51-*.md` | Juan Carlos Alvarez Lagos / Claude |
| 2026-07-23 | Implementation / Implementación | T7 completado: el dashboard HTML (`packages/sdd-mcp/src/dashboard.ts`) muestra el badge de deriva `🔀 N` junto a los de gate y dependencias, con tooltip de los commits, ES/EN. Verificado en vivo sobre un workspace git de demo (`001-pagos` con `🔀 1`). La 025 queda completa (9 de 9 tareas). | `packages/sdd-mcp/src/dashboard.ts` | Juan Carlos Alvarez Lagos / Claude |
