# Change history / Historial de cambios

| Date / Fecha | Change type / Tipo de cambio | Summary / Resumen | Files impacted / Archivos impactados | Owner / Responsable |
|---|---|---|---|---|
| 2026-08-30 | Scope / Alcance | Borrador inicial a partir del bloque A de `idea/IDEAS_BUILDER_V4_2026-08-30.md`: siete rutas de pérdida de datos reproducidas y medidas sobre `a934896`, más la deuda que la spec 041 dejó abierta en su decisión 8 | `spec.md`, `plan.md`, `tasks.md`, `research.md` | Juan Carlos Alvarez Lagos / Claude |
| 2026-08-30 | Alcance / Scope | Partido en dos antes de proponerse: el paquete original tenía 11 requisitos y 279 líneas, contra el listón «1-3 páginas» de `.claude/commands/sdd/spec.md`. Toda la concurrencia —serialización canónica, identificador de versión y 409, guarda de eco por identidad, presencia y líder entre pestañas— sale a la spec siguiente. Aquí quedan las cinco rutas de «destruir lo que no se entendió» | `spec.md`, `plan.md`, `tasks.md`, `research.md` | Juan Carlos Alvarez Lagos / Claude |
| 2026-08-30 | Corrección / Correction | Revisión por tres lentes contra el listón del repo. Corregidos: un valor falso en «medido, no supuesto» (marco de 760×320 → 560×360 real, `convert.ts:27`); dos afirmaciones inventadas sobre el «fuera de alcance» de la spec 024 en `research.md`; una inversión de dependencia entre paquetes en el plan (`sdd-core` no puede importar `canvasSchema` de `sdd-mcp`); y el choque de orden entre validar tipos desconocidos y añadir el nodo `link`. Añadidas las tareas de prueba de los criterios que no tenían ninguna | `spec.md`, `plan.md`, `tasks.md`, `research.md` | Juan Carlos Alvarez Lagos / Claude |
| 2026-08-30 | Aprobación / Approval | Aprobada contra el `spec.md` del 2026-08-30, con las cinco rutas y los siete requisitos. Consentimiento registrado en `.sdd/user-consent.log` como acto separado | `spec.md`, `.sdd/user-consent.log` | Juan Carlos Alvarez Lagos |
| 2026-08-30 | Implementación / Implementation | Fases 1-6: red de pruebas del store (6 casos, verificada rompiendo el orden a propósito), nodo `link` y `subpath` en las tres declaraciones, mecanismo `extra` generalizado a nodos y aristas, `validateCanvas` en el núcleo, lectura que distingue ausente de ilegible con pantalla propia y respaldo `.bak`, estado `error` con reintento, y guardia de atajos extraída a `shortcuts.ts` | `builder/src/*`, `packages/sdd-core/src/board.ts`, `packages/sdd-mcp/src/{api,events,schemas}.ts`, `docs/*/54-*.md` | Juan Carlos Alvarez Lagos / Claude |
| 2026-08-30 | Alcance / Scope | Tres cambios sobre lo aprobado. (1) `@testing-library/react` descartado: las seis pruebas de la fase 1 son de estado y el entorno `jsdom` ya existía; añadir dos dependencias sin uso habría sido deuda gratuita. (2) `.passthrough()` en `canvasNodeSchema` y `canvasEdgeSchema`, no previsto en las tareas: sin él la ruta MCP seguía borrando las claves que el builder acababa de aprender a conservar. (3) La guardia de atajos distingue el panel de spec (`data-shortcuts="allow"`) de los modales, porque el cajón es `modal={false}` y bloquearlo habría apagado los atajos en el uso normal — comprobado en el navegador | `tasks.md`, `packages/sdd-mcp/src/schemas.ts`, `builder/src/components/SpecDrawer.tsx` | Juan Carlos Alvarez Lagos / Claude |
| 2026-08-30 | Aclaración / Clarification | El criterio EARS del reintento dice «declarar el error tras el tercer fallo»; se implementó la lectura de `tasks.md`, que es la precisa: tres reintentos (250 ms, 1 s, 4 s) y estado `error` al cuarto fallo | `builder/src/store.ts` | Juan Carlos Alvarez Lagos / Claude |
| 2026-08-30 | Verificación / Verification | 307 pruebas en verde y typecheck limpio en los tres paquetes. Verificado además en el navegador contra el servidor real: un canvas con `link`, `subpath`, clave desconocida, grupo y arista con lados sobrevive a un arrastre; un `board.canvas` con marcadores de conflicto abre la pantalla nueva sin escribir nada y «descartar» conserva el archivo en `.bak`; con el tour, el menú ⋯ y la paleta abiertos ningún atajo crea nodos, y con el cajón abierto sí | — | Juan Carlos Alvarez Lagos / Claude |
| 2026-08-30 | Cierre / Closure | `STATUS.md` regenerado (33 → 43 filas, con las specs 034-042 que faltaban) ejecutando el generador real sobre un espejo de `specs/` y `bitacora/`, porque `generateStatus()` rechaza la raíz del template. El rodeo queda recogido como spec 043 | `STATUS.md`, `tasks.md` | Juan Carlos Alvarez Lagos / Claude |
