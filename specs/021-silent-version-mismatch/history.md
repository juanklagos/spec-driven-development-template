# Change history / Historial de cambios

| Date / Fecha | Change type / Tipo de cambio | Summary / Resumen | Files impacted / Archivos impactados | Owner / Responsable |
|---|---|---|---|---|
| 2026-07-22 | Scope / Alcance | Borrador inicial. Nace de un reporte del autor: `npx @juanklagos/sdd-mcp --http` en `~/www/larepolla` devolvía 0 bytes y exit 0. La causa (caché de npx con la 2.2.0) se rodeó por documentación; esta spec ataca el silencio del binario, que se midió también en la 2.2.1. | `spec.md`, `plan.md`, `tasks.md`, `research.md` | Juan Carlos Alvarez Lagos / Claude |
| 2026-07-23 | Implementation / Implementación | Aprobada y consentida. Nuevo `packages/sdd-mcp/src/cli.ts` (`parseCliArgs` + `helpText` + `unknownArgMessage` + `packageVersion`); `index.ts` despacha sobre la intención. `http.ts` envuelve el arranque en try/catch (nombra puerto + `SDD_MCP_HTTP_PORT`, `process.exit(1)`) e instala el manejador de `uncaughtException` solo tras escuchar. Verificado ejecutando: `--help`/`--version` exit 0, `--htp` exit 1 con la versión en stderr y stdout vacío, `--http` con el rango ocupado exit 1 sin «keeps running», stdio sin args stdout 0 bytes. Cobertura en el smoke del paquete empaquetado (5 casos) + prueba unitaria `cli.test.ts` (Vitest). README y decisión (`2026-07-23-el-binario-falla-en-voz-alta.md`). T10 (paquete publicado) la ejecuta el propietario con OTP. | `packages/sdd-mcp/src/{cli.ts,cli.test.ts,index.ts,http.ts,tsconfig.json}`, `packages/sdd-mcp/README.md`, `scripts/smoke-test-npm-package.mjs`, `bitacora/decisiones/2026-07-23-el-binario-falla-en-voz-alta.md` | Juan Carlos Alvarez Lagos / Claude |
