# Change history / Historial de cambios

| Date / Fecha | Change type / Tipo de cambio | Summary / Resumen | Files impacted / Archivos impactados | Owner / Responsable |
|---|---|---|---|---|
| 2026-07-22 | Scope / Alcance | Borrador inicial tras el barrido de 84 agentes: 71 hallazgos confirmados, y los cinco de esta spec verificados a mano. Dos de ellos son trabajo del día anterior que no funciona: el aviso de consentimiento heredado diseñado para el caso equivocado, y el sello de versión que las reglas de ignorado que yo mismo escribí dejan fuera de git. | `spec.md` | Juan Carlos Alvarez Lagos |
| 2026-07-22 | Implementation / Implementación | H1 y H2. El sello sale del ignorado y lo escriben los dos andamiadores con formato ISO-UTC unificado. El aviso de consentimiento heredado pasa a ser **temporal**: un consentimiento anterior a `installed_at` no puede ser del dueño del workspace. Sin sello, silencio — el propio template no se acusa. La tubería de `validate-sdd.sh` deja de abortar bajo `set -euo pipefail`. Idea y bitácora se crean vacías en vez de copiar las mías. Verificado sobre una copia real del repositorio: avisa nombrando 25 entradas heredadas; sobre el template: calla. | `scripts/{validate-sdd,check-sdd-gate,install-spec-sidecar,init-project}.sh`, `scripts/lib/sdd-scaffold.sh` | Claude |
| 2026-07-22 | Implementation / Implementación | H3 y los siguientes-pasos que no funcionaban. `demo.tape` andamia antes de ejecutar: la cinta corría `new-spec.sh` en la raíz del template, donde el propio script se niega por diseño, así que el GIF de portada grababa su propio primer comando fallando y el release lo re-commiteaba. `create-www-project.sh` calcula el prefijo de scripts según el perfil —el perfil `full` instala en la raíz y la cinta imprimía `./spec/scripts/` para ambos, así que sus cinco comandos salían con 127. Backticks escapados en el heredoc sin comillas de `init-project.sh`, que emitía dos errores de bash y borraba el texto que anunciaba. `reset-template.sh` deja de mandar a `new-spec.sh`, que se niega ahí. Ejemplos de consentimiento con slug completo. `.mcp.json` apunta al bin publicado en vez de a un `dist/` que un clon fresco no tiene, y `prepare` construye al instalar. | `demo.tape`, `scripts/{create-www-project,init-project,reset-template}.sh`, `.mcp.json`, `package.json` | Claude |
| 2026-07-22 | Implementation / Implementación | `missing-consent-log` se emitía con `path: ".sdd/user-consent.log"`, que no puede coincidir con `SPEC_PATH_RE`, así que caía en el error global — y el builder no renderiza errores globales en ningún sitio. Resultado: para la PRIMERA spec aprobada de un proyecto nuevo, donde el registro aún no existe, la tarjeta se ponía roja sin mensaje y sin salida, que es exactamente el caso para el que se hizo el panel de consentimiento de la 015. Ahora se emite además un `missing-spec-consent` por spec aprobada. Y la ruta B del QUICKSTART, que usaba rutas standalone tras instalar un sidecar: cada comando salía con 127. Ejemplos de consentimiento con slug completo en los nueve archivos que los tenían. | `packages/sdd-core/src/index.ts`, `QUICKSTART.md`, `AGENTS.md`, `INSTRUCTIONS.md`, `README.md`, `README.es.md`, `templates/sidecar/AGENTS.md`, `docs/{en,es}/40-*` | Claude |
| 2026-07-22 | Implementation / Implementación | `START_HERE_NON_TECH.md` reescrito. Mandaba usar `www/` como raíz de ejecución, contradiciendo al README y al registro del 2026-03-18 que retiró esa obligación — cuatro meses desfasado, y es la primera de las tres puertas que el README ofrece. Ahora no pide al lector elegir dónde va nada: el prompt manda andamiar con `create-sdd-project` o instalar el sidecar, y el propio andamiador imprime los pasos según el perfil. Añadida la separación aprobar/consentir y el acceso al lienzo. **Hallazgo al escribirlo: el builder NO viaja en la 2.1.0 publicada** (72K, cero archivos de frontend) aunque sí en el tarball local (304K) — se añadió después de publicar. El documento lo dice y avisa del 503. | `START_HERE_NON_TECH.md` | Claude |
| 2026-07-22 | Documentation / Documentación | Las cinco capturas de la guía 51 regeneradas. Tres mostraban la ruta del scratchpad temporal donde se tomaron (`…/claude-501/…`), es decir la máquina de quien las sacó, y ninguna mostraba el veredicto de tres estados de la 012. Se tomaron sobre dos workspaces andamiados de verdad: `canvas`/`drawer`/`kanban` sobre una tienda con cuatro specs en los tres estados (Pendiente, Aprobada, Hecha) y las tres clases de unión; `templates`/`assistant` sobre uno recién creado, porque ambas pantallas solo se aplican a un workspace vacío y contra la tienda salían con el banner rojo. **Al montarlas encontré por qué el aviso de dependencias no aparecía: escribí el tablero a mano con etiquetas bilingües (`depende de / depends on`), y `classifyEdgeLabel` solo acepta la forma combinada para `contiene`.** Mis uniones se degradaban a `related` en silencio: sin color, sin aviso. Las capturas usan ahora las etiquetas que escribe el propio builder. Corregidos los textos alternativos y el ejemplo del aviso, que nombraban specs y estados que ya no salen. | `docs/assets/builder/*.png`, `docs/{es,en}/51-*` | Claude |
