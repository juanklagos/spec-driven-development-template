# Change history / Historial de cambios

| Date / Fecha | Change type / Tipo de cambio | Summary / Resumen | Files impacted / Archivos impactados | Owner / Responsable |
|---|---|---|---|---|
| 2026-08-30 | Scope / Alcance | Borrador inicial. Origen: los cuatro bloqueos encontrados al implementar la spec 042 —`new-spec.sh`, `sdd_create_spec`, `generateStatus` y `scoreSpec` rechazando la raíz del template— y el hallazgo G.3 de `idea/IDEAS_BUILDER_V4_2026-08-30.md` | `spec.md`, `plan.md`, `tasks.md`, `research.md` | Juan Carlos Alvarez Lagos / Claude |
| 2026-08-30 | Aprobación / Approval | Aprobada contra el `spec.md` del 2026-08-30, con los cuatro requisitos y las tres fases. Consentimiento registrado aparte | `spec.md`, `.sdd/user-consent.log` | Juan Carlos Alvarez Lagos |
| 2026-08-30 | Implementación / Implementation | Fases 1-3: 11 pruebas en `workspace.test.ts` (las cuatro reglas que sobreviven, los tres rechazos que siguen vivos y las dos lecturas que se ganan), guarda retirada de `resolveSddRoot` y añadida explícitamente en `createWorkspace`, y bloque equivalente retirado de `new-spec.sh` | `packages/sdd-core/src/{workspace,index}.ts`, `packages/sdd-core/src/workspace.test.ts`, `scripts/new-spec.sh` | Juan Carlos Alvarez Lagos / Claude |
| 2026-08-30 | Alcance / Scope | Dos ajustes sobre lo planeado. (1) `installSidecar` y `runLegacyDiscovery` YA llamaban a la guarda por su cuenta, así que R2 sólo requirió tocar `createWorkspace`; se comprueba con prueba propia que las tres siguen rechazando. (2) R3 decía «conservando su bloqueo para los demás casos que ya cubre»: al leerlo, la condición del script sólo podía ser cierta para la raíz del template, así que no había otros casos y el bloque se retira entero | `tasks.md`, `scripts/new-spec.sh` | Juan Carlos Alvarez Lagos / Claude |
| 2026-08-30 | Verificación / Verification | Los cuatro bloqueos del contexto dejan de fallar, comprobados sobre este repositorio: `./scripts/new-spec.sh` creó la spec 044 de prueba (borrada después), `listSpecs` devuelve 44, `scoreSpec` puntúa la 043 en 89/100 y `generateStatus` reescribe `STATUS.md` sin espejo. 313 pruebas en verde y typecheck limpio | — | Juan Carlos Alvarez Lagos / Claude |

