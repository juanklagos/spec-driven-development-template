# Change history / Historial de cambios

| Date / Fecha | Change type / Tipo de cambio | Summary / Resumen | Files impacted / Archivos impactados | Owner / Responsable |
|---|---|---|---|---|
| 2026-07-22 | Implementation / Implementación | H1-H3. Las guías de ambos idiomas se publican con el mismo nombre de archivo, emparejadas por número, que es lo único que hace que Starlight las reconozca como traducciones. El menú autogenerado se sustituye por siete grupos curados con etiqueta traducida, declarados una sola vez en `site/src/guides.mjs` e importados por el sincronizador y por la configuración, con una comprobación que **falla la construcción** si una guía queda fuera o en dos grupos. Las URLs no se adivinan a partir del nombre: dos guías (`39`, `46`) llevan puntos y Astro los borra vía `github-slugger`, así que el menú y las 51 redirecciones usan esa misma función. Verificado sobre `dist`: 52 entradas por idioma, cero cruzadas, cero enlaces internos rotos. | `site/src/guides.mjs`, `site/astro.config.mjs`, `site/scripts/sync-docs.mjs`, `site/package.json` | Claude |
| 2026-07-22 | Scope / Alcance | Borrador inicial. El autor reportó el menú del sitio «revuelto» y mezclando idiomas. Medido sobre `site/dist`: el menú español trae 103 entradas en vez de 52, todas bajo `/es/`, porque Starlight empareja traducciones por ruta de archivo y las guías españolas se publican con nombre español. `09-release-checklist` —única con nombre idéntico en ambos idiomas— es la única que no se duplica, lo que confirma la regla. | `spec.md`, `plan.md`, `tasks.md`, `research.md` | Claude |
