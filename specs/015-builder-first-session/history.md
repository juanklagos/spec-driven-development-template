# Change history / Historial de cambios

| Date / Fecha | Change type / Tipo de cambio | Summary / Resumen | Files impacted / Archivos impactados | Owner / Responsable |
|---|---|---|---|---|
| 2026-07-21 | Scope / Alcance | Borrador inicial. Tres hallazgos verificados: el título humano existe en la línea 1 y se descarta, no hay ruta de consentimiento en la API pese a que el core la soporta, y no hay forma de llegar a una spec sin barrer con el ratón. | `spec.md`, `plan.md`, `tasks.md`, `research.md` | Juan Carlos Alvarez Lagos |
| 2026-07-21 | Approval / Aprobación | Spec aprobada por el autor en chat. Consentimiento registrado en `.sdd/user-consent.log`. | `spec.md` (bloque de estado) | Juan Carlos Alvarez Lagos |
| 2026-07-21 | Implementation / Implementación | H1: `extractSpecTitle` con degradación en tres escalones (encabezado con guion → encabezado completo → etiqueta derivada del id), detectando marcadores de plantilla sin rellenar. `SpecSummary.title` poblado sin I/O adicional, expuesto en los esquemas MCP y usado en las tarjetas del lienzo y del kanban con el id visible al lado. **El guardián de forma se reescribió**: derivaba de una lista escrita a mano, así que solo cazaba los campos que alguien recordara añadirle — la misma clase de omisión que existe para prevenir. Ahora deriva de un `SpecSummary` real y cazó `title` sin que nadie se lo dijera. | `packages/sdd-core/src/{workspace,index}.ts`, `packages/sdd-mcp/src/{schemas,server}.ts`, `builder/src/{types,components/SpecNode,components/KanbanBoard}.tsx`, `scripts/test-mcp-integration.mjs` | Claude |
| 2026-07-21 | Implementation / Implementación | H2: `POST /api/spec/:id/consent` con identificador explícito en la URL y resumen en el cuerpo; 400 si falta. Cliente en el builder y panel en la pestaña de aprobación que **solo aparece cuando la compuerta dice que falta** —para que no sea un botón que se pulsa por reflejo— con un texto que explica que aprobar y consentir son dos decisiones distintas. Verificado de punta a punta: compuerta BLOQUEADA antes, POST 200, compuerta ABIERTA después, con la línea marcada `[spec:001-cobros]`. | `packages/sdd-mcp/src/api.ts`, `builder/src/api.ts`, `builder/src/components/SpecDrawer.tsx`, `builder/src/i18n.ts` | Claude |
