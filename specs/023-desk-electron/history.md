# Change history / Historial de cambios

| Date / Fecha | Change type / Tipo de cambio | Summary / Resumen | Files impacted / Archivos impactados | Owner / Responsable |
|---|---|---|---|---|
| 2026-07-22 | Implementation / Implementación | Ícono de marca y empaquetado (T9 parcial). `desk/build/icon.svg` deriva del logo de la spec 018 con una diferencia deliberada: el mark recorta la S y deja pasar el fondo, y su tinta cambia con `prefers-color-scheme` — ninguna de las dos cosas sirve para un ícono de Dock, que es un ráster estático sobre un fondo que nadie eligió. El ícono invierte el tratamiento: cuadro tinta, glifo claro, las mismas dos tintas de marca, sin inventar una tercera ni tocar el verde que la 018 reserva para estado. `scripts/build-icon.mjs` rasteriza con Electron (exacto y reproducible) en vez de `qlmanage` (solo macOS, tamaño a su criterio). `electron-builder.yml` + entitlements. **Verificado:** `.icns` de 175 KB dentro del bundle con el monograma, y la app empaquetada arranca y sirve `/builder` con 200. Firma y notarización siguen pendientes de credenciales. | `desk/build/**`, `desk/scripts/build-icon.mjs`, `desk/electron-builder.yml`, `desk/src/main/{assets.ts,window.ts,main.ts}` | Claude |
| 2026-07-22 | Fix / Corrección | El generador de íconos moría sin excepción, sin stderr, con código de salida 0 y dejando `icon.png` en 0 bytes. Dos causas encadenadas: la imagen de `capturePage` está respaldada por recursos de la ventana, así que `resize()` después de `window.destroy()` tumba el proceso entero; y Electron se traga el `stdout` del script, de modo que el fallo era además invisible. `withRender(svg, use)` hace que la ventana sobreviva a todo uso de la imagen por construcción y no por recordar el orden de dos sentencias; los logs van a stderr; y `writePng` falla si `toPNG()` devuelve 0 bytes, porque `writeFile` trunca antes de escribir. | `desk/scripts/build-icon.mjs` | Claude |
| 2026-07-22 | Implementation / Implementación | T1-T7 completos (falta T8-T13). `createSddHttpServer` extraída con fallback de puerto; `http.ts` queda como CLI y conserva el banner, los handlers de proceso y su bin. `desk/` con main de Electron que hospeda el servidor en proceso y carga `builder/dist` verbatim. **Verificado en ejecución:** la app arrancó, se corrió sola de 3334 (ocupado por un `npx --http` del autor) a 3335 y lo anunció; la ventana renderizó el board de `larepolla`; una escritura con origen loopback devolvió 200 y la misma con `Origin: null` devolvió 403 — el criterio de aceptación sobre `file://` era real, no teórico; el cambio de workspace en caliente relevantó el servidor sin reiniciar la app. | `packages/sdd-mcp/src/{http-server.ts,http.ts}`, `desk/**`, `scripts/smoke-test-mcp-http.mjs` | Juan Carlos Alvarez Lagos / Claude |
| 2026-07-22 | Fix / Corrección | Dos defectos encontrados por la verificación, no por revisión. (1) `mcp:http:smoke` llevaba rota desde la spec 020: esperaba el string `"SDD MCP Streamable HTTP server listening"`, que `git grep` solo encuentra dentro del propio test — agotaba 15 s en cada corrida sin señalar nada. Ahora la disponibilidad se mide contra el puerto, y los puertos se reservan del SO en vez de fijarse en 3334-3337. (2) La validación de workspace de `desk` duplicaba la regla de layout y aceptaba la raíz del template, que `sdd-core` rechaza: el error aterrizaba dentro de la página en vez del diálogo. Ahora delega en `ensureProjectRootAllowed` y muestra su razón textual. | `scripts/smoke-test-mcp-http.mjs`, `desk/src/main/{workspace.ts,dialogs.ts}` | Claude |
| 2026-07-22 | Scope / Alcance | Borrador inicial. `desk/` como carpeta hermana de `builder/`: shell de Electron que hospeda el servidor SDD en proceso y carga `builder/dist` verbatim; incluye el endpoint MCP de la app como superficie visible. Redactada tras la evaluación de la sesión, que encontró una decisión vigente en contra (`2026-07-21-no-app-escritorio.md`) con sus fases previas sin completar — el propietario reafirmó el pedido con esa evaluación a la vista. Decisión registrada aparte. | `spec.md`, `plan.md`, `tasks.md`, `research.md` | Juan Carlos Alvarez Lagos / Claude |
| 2026-07-22 | Release / Publicación | T11 y T12. SDD Desk 0.1.0 publicado: `.dmg` arm64 y x64, `.exe` NSIS, `AppImage` y `.deb`, con página de descarga EN/ES en el sitio. **Tres defectos que encontró la verificación, no la revisión.** (1) El workflow llamaba a `electron-builder` sin compilar TypeScript: el asar salía sin entrypoint y fallaban las tres plataformas con "corrupted archive", que nombra el síntoma y no la causa. (2) Faltaban `author` y `homepage`; sin ellos Linux no empaqueta y el `.deb` no tiene Maintainer — un hueco que solo aparecía en un tercio de la matriz. (3) El publisher de electron-builder deriva el tag del release de `package.json` e ignora el tag de git: `desk-v0.1.0` produjo un release tagueado `v0.1.0`, en el mismo espacio que los `v2.x` del template, y al publicarlo se habría llevado el puntero "Latest". Ahora las builds no publican; un job final sube con `gh` a un release de vida larga bajo el tag fijo `desk-latest`, `latest=false`, cuyos assets se reemplazan por versión — eso hace permanente la URL sin que el escritorio posea un puntero que no le corresponde. Verificado descargando desde el enlace público. Sigue sin firmar: R8 abierto. | `.github/workflows/desk-release.yml`, `desk/{package.json,electron-builder.yml}`, `site/**` | Claude |
| 2026-07-23 | Implementation / Implementación | T8, T10 y T13. **T10 (R9):** `updater.ts` con `electron-updater` apuntando al tag fijo `desk-latest` vía el proveedor `generic` — el proveedor `github` resuelve el "latest" del repositorio, que el escritorio deliberadamente no posee. Nunca fuerza: `autoDownload` y `autoInstallOnAppQuit` en falso, la comprobación es silenciosa salvo que haya algo que decir, y "ahora no" significa ahora no. En macOS no descarga: Squirrel.Mac verifica la firma y rechaza un bundle sin firmar, así que abrir la página de descarga es lo honesto en vez de correr una descarga que fallará en el último paso. Feed verificado sirviendo `latest-mac.yml`, `latest.yml` y `latest-linux.yml` con sus sha512. **T8:** paridad H3 verificada — `/api/board` y `/api/gate` idénticos byte a byte entre la app y la ruta de navegador sobre el mismo proyecto. H4 verificada con `scripts/verify-desk-agent-write.mjs`: un agente externo conectado al endpoint MCP de la app creó una spec, llegó a disco, el stream SSE emitió `change` y quedó visible en el board de la app. **T13:** la app empaquetada arranca y sirve `/builder` con `node` y `npm` fuera del `PATH`. | `desk/src/main/{updater.ts,i18n.ts,main.ts}`, `desk/electron-builder.yml`, `scripts/verify-desk-agent-write.mjs` | Claude |
| 2026-07-23 | Fix / Corrección | La app empaquetada y la de desarrollo guardaban sus ajustes en dos carpetas distintas. `app.setName()` no mueve `userData`: Electron lo resuelve desde el nombre del paquete antes de que esa línea corra, así que la build empaquetada escribía en `sdd-desk` y la de desarrollo en `SDD Desk`. La app recordaba un proyecto distinto según cómo se lanzara, y solo la build empaquetada lo mostraba — no habría aparecido nunca en desarrollo. Ahora la ruta se fija explícitamente con `app.setPath`. Encontrado probando el `.app` real, no leyendo el código. | `desk/src/main/main.ts` | Claude |
