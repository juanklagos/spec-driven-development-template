# History 010

## 2026-07-21

- Spec creada desde feedback de uso real del autor; aprobada en el mismo mensaje; consentimiento registrado.

## 2026-07-21 (implementación)

- R1 i18n real: `builder/src/i18n.ts` (diccionario plano es/en + zustand + `useT()`/`translate()`, detección por `navigator.language`, switcher ES/EN en TopBar persistido en localStorage, `<html lang>` sincronizado). Cero textos bilingües simultáneos en toda la UI: TopBar, paleta, tour, modales, sheet, kanban, toasts, errores de cliente, tooltips, wizard, aria-labels, plantillas, asistente y prompts copiables (ahora en un solo idioma). Limitación honesta: los errores que vienen del SERVIDOR (API/MCP) siguen bilingües.
- R4 shadcn/ui + Tailwind v4: flujo oficial (`npx shadcn add` con `components.json` para Vite) — button, dialog, tabs, sheet, badge, tooltip, select, accordion, sonner, separator, scroll-area en `builder/src/components/ui/`. Tema oklch con el verde 🌱 como `--primary`, light/dark por `prefers-color-scheme` (sin toggle, igual que React Flow `colorMode="system"`). React Flow intacto.
- R2 editor completo: `updateSpecSections` (sdd-core) cubre TODO el template — historia, escenarios, criterios EARS, requisitos, propiedades, criterios de éxito, fuera de alcance; `approveSpec` acepta `evidence` opcional (siempre gana; sin ella no pisa evidencia existente). Sheet ancho con tabs Resumen / Editar spec (accordion con add/remove/reorder + lint EARS localizado) / Aprobación (estado y fecha readonly + aprobador + evidencia) / Relaciones. API y tools MCP extendidas (`PUT /sections`, `POST /approve`, `sdd_update_spec_sections`, `sdd_approve_spec`).
- R3 uniones con propósito: selector inmediato al crear la unión (contiene gris, depende de ámbar, bloquea rojo, relacionada azul por defecto, etiqueta libre) con icono+color en el edge; `contiene`/`contains` es canónico también en sdd-core (`classifyEdgeLabel`, color hex `#6b7280`; nunca genera dependencyWarnings). Panel Relaciones con entrantes/salientes, cambio de tipo y eliminar.
- R5: guía 51 EN/ES con la sección «Conecta tu agente» (Claude Code `claude mcp add`, Codex config.toml, Gemini CLI settings.json, Claude Desktop/ChatGPT conector HTTP `/mcp` + MCP App) y nota de que los cambios del agente se ven en vivo en `/builder`.
- Verificación: builds+typecheck raíz y builder verdes; `mcp:test` extendido (secciones nuevas en disco, evidencia que pisa la línea, color de `contiene`) verde; 3 scripts SDD a 0 errores; sandbox sidecar fresco + navegador en :3399 — UI 100% ES y switch total a EN, secciones editadas por forms verificadas en spec.md con aprobación intacta, aprobación con evidencia escrita en disco, unión creada → selector → `contiene` + `#6b7280` persistidos en board.canvas, undo/redo conserva el edge, smoke de tour/plantillas/asistente/kanban/gate/PNG y dark mode. Las 5 capturas de `docs/assets/builder/` regeneradas con Playwright (1280×800 @2x, ES, demo tienda de plantas con gate abierto + ⚠ 1 dep).
