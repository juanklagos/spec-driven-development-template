# History 010

## 2026-07-21

- Spec creada desde feedback de uso real del autor; aprobada en el mismo mensaje; consentimiento registrado.

## 2026-07-21 (implementación)

- R1 i18n real: `builder/src/i18n.ts` (diccionario plano es/en + zustand + `useT()`/`translate()`, detección por `navigator.language`, switcher ES/EN en TopBar persistido en localStorage, `<html lang>` sincronizado). Cero textos bilingües simultáneos en toda la UI: TopBar, paleta, tour, modales, sheet, kanban, toasts, errores de cliente, tooltips, wizard, aria-labels, plantillas, asistente y prompts copiables (ahora en un solo idioma). Limitación honesta: los errores que vienen del SERVIDOR (API/MCP) siguen bilingües.
- R4 shadcn/ui + Tailwind v4: flujo oficial (`npx shadcn add` con `components.json` para Vite) — button, dialog, tabs, sheet, badge, tooltip, select, accordion, sonner, separator, scroll-area en `builder/src/components/ui/`. Tema oklch con el verde 🌱 como `--primary`, light/dark por `prefers-color-scheme` (sin toggle, igual que React Flow `colorMode="system"`). React Flow intacto.
- R2 editor completo: `updateSpecSections` (sdd-core) cubre TODO el template — historia, escenarios, criterios EARS, requisitos, propiedades, criterios de éxito, fuera de alcance; `approveSpec` acepta `evidence` opcional (siempre gana; sin ella no pisa evidencia existente). Sheet ancho con tabs Resumen / Editar spec (accordion con add/remove/reorder + lint EARS localizado) / Aprobación (estado y fecha readonly + aprobador + evidencia) / Relaciones. API y tools MCP extendidas (`PUT /sections`, `POST /approve`, `sdd_update_spec_sections`, `sdd_approve_spec`).
- R3 uniones con propósito: selector inmediato al crear la unión (contiene gris, depende de ámbar, bloquea rojo, relacionada azul por defecto, etiqueta libre) con icono+color en el edge; `contiene`/`contains` es canónico también en sdd-core (`classifyEdgeLabel`, color hex `#6b7280`; nunca genera dependencyWarnings). Panel Relaciones con entrantes/salientes, cambio de tipo y eliminar.
- R5: guía 51 EN/ES con la sección «Conecta tu agente» (Claude Code `claude mcp add`, Codex config.toml, Gemini CLI settings.json, Claude Desktop/ChatGPT conector HTTP `/mcp` + MCP App) y nota de que los cambios del agente se ven en vivo en `/builder`.
- Verificación: builds+typecheck raíz y builder verdes; `mcp:test` extendido (secciones nuevas en disco, evidencia que pisa la línea, color de `contiene`) verde; 3 scripts SDD a 0 errores; sandbox sidecar fresco + navegador en :3399 — UI 100% ES y switch total a EN, secciones editadas por forms verificadas en spec.md con aprobación intacta, aprobación con evidencia escrita en disco, unión creada → selector → `contiene` + `#6b7280` persistidos en board.canvas, undo/redo conserva el edge, smoke de tour/plantillas/asistente/kanban/gate/PNG y dark mode. Las 5 capturas de `docs/assets/builder/` regeneradas con Playwright (1280×800 @2x, ES, demo tienda de plantas con gate abierto + ⚠ 1 dep).

## 2026-07-21 (seguimiento: el dashboard y la regla de estado)

- El autor señaló que `/dashboard` había quedado "muy básico" frente al builder ya migrado a shadcn. Rediseñado con un panel de jueces de diseño (3 propuestas independientes → evaluación → síntesis): capa ejecutiva, banda de compuerta, KPIs, lista de specs con progreso, avisos de dependencias, tokens exactos del builder, dark/light, y un solo idioma (`?lang` → `Accept-Language` → es) con toggle ES|EN.
- Hallazgo durante la verificación: la regla de estado de una spec estaba duplicada en cuatro sitios y **divergía**. `SpecNode` usaba `/aprobado|approved/i` (no reconocía "Aprobada"), el kanban `/aprobad[oa]|approved/i`, el drawer una tercera copia para habilitar "Implementar con agente", y el dashboard exigía aprobación para "hecha". Consecuencia real: una spec "Aprobada" salía pendiente en el lienzo pero aprobada en el kanban; y una spec con todas las tareas marcadas sin aprobar salía "Hecha" en el lienzo y "Pendiente" en el dashboard.
- Corregido en la fuente única: `specTone` en `sdd-core` (aprobación primero — sin aprobación no hay "hecha", que es justo el anti-patrón que el template existe para exponer), expuesto como `BoardSpecCard.tone` por la API REST y las tools MCP; lienzo, kanban, drawer y dashboard solo lo pintan. Único espejo documentado: `sections.ts` para la pestaña que lee el markdown crudo.
- Verificado con los dos casos borde en un workspace real: API, dashboard, tarjeta del lienzo y columna del kanban coinciden (capturas). Asserts de regresión añadidos a `mcp:test`; el esquema de salida de las tools MCP incluye `tone`.

## 2026-07-21 (seguimiento: R1 cierra la última excepción — errores del servidor)

- La entrada de implementación de R1 declaraba una limitación honesta: «los errores que vienen del SERVIDOR (API/MCP) siguen bilingües». Esa excepción contradecía el propio R1 («cero labels dobles en toda la UI (incl. tour, toasts, errores)») y era visible: `POST /api/spec/:id/issues` en un workspace sin git respondía `422 {"error":"Este workspace no es un repositorio git / This workspace is not a git repository — ejecuta / run: git init && git remote add origin <url>"}` y `SpecDrawer` lo pintaba tal cual.
- Cerrada sin duplicar la regla: `packages/sdd-mcp/src/github.ts` define la taxonomía (`GITHUB_ERROR_CODES` + `GithubPreconditionError` con `code` y `detail`), `api.ts` devuelve `{ error, code, detail? }`, y el cliente del builder traduce `error.code.<CODE>` con el diccionario que ya existía. El servidor sigue siendo el dueño del significado; la UI solo elige idioma. El mensaje bilingüe permanece como *fallback* para clientes sin diccionario (curl, tools MCP).
- Regresión: `scripts/smoke-test-mcp-http.mjs` exige `code === "GH_NO_REPO"` en esa ruta (falla con el cuerpo antiguo, verificado), y `scripts/test-mcp-integration.mjs` exige que TODO código que el servidor pueda emitir tenga entrada `error.code.<CODE>` en `builder/src/i18n.ts` (la paridad es/en existente cubre los dos idiomas).
- R1 queda cumplido sin asterisco. Los errores de precondición sin código (p. ej. `Invalid spec id`) siguen mostrándose tal cual: son de programación, no de usuario.
