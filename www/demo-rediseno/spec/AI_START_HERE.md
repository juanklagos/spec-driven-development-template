# AI Start Here

This project keeps its SDD operating system in `./spec/` so the project root stays clean.

Canonical source:
- `template-context/core-instructions/AGENT_OPERATING_SYSTEM.md`

No code before approved spec and consistent plan.
Use spec/ as the SDD sidecar and keep runnable code in project root.

Think about it like this:
- project root = app code
- `spec/` = idea, specs, logbook, validation, and AI guidance

Before doing implementation work:
1. Read `./spec/idea/IDEA_GENERAL.md`
2. Read `./spec/specs/INDEX.md`
3. Work from one active spec
4. Do not implement without approved spec and consistent plan
5. Record user consent right before implementation

Do not clone or copy the whole framework repository into this project unless the user explicitly asks for that mode.
