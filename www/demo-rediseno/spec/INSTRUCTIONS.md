# Sidecar Instructions

This project uses the compact `spec/` sidecar layout.

Canonical source:
- `template-context/core-instructions/AGENT_OPERATING_SYSTEM.md`

No code before approved spec and consistent plan.
Use spec/ as the SDD sidecar and keep runnable code in project root.
This is the recommended default workspace inside a real project.

- Code lives in the project root.
- SDD lives in `./spec/`.
- Use the framework repository as canonical reference, not as something to clone into this project.
- Keep changes in `./spec/idea`, `./spec/specs`, and `./spec/bitacora` aligned.
- No code before approved spec and consistent plan.
